window.onload = function () {
  var count = document.getElementById('count');
  var indexBody = document.getElementById('indexBody');
  window.onscroll = function() {
  	var scrolled = window.pageYOffset;
    //count.innerHTML = scrolled;
  }
}